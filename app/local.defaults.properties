# Checked-in fallback for the Secrets Gradle Plugin so the project still builds (with Maps
# disabled/broken at runtime) before you've added your own key. Put your REAL key in
# app/secrets.properties instead - that file is gitignored and overrides this one.
MAPS_API_KEY=DEFAULT_API_KEY

# Checked-in fallback so the project still builds before you've added your own values. This file
# IS committed to git, so never put a real key/secret in it - put real values in
# app/secrets.properties instead (gitignored, overrides this file).
MAPS_API_KEY=DEFAULT_API_KEY
UPI_PAYEE_VPA=example@upi
UPI_PAYEE_NAME=Amchi Yatri
